# Project Log — 2026-07

## 2026-07-14

### App Store Screenshot Template

- Added three en-US Mac App Store marketing screenshots for batch import and
  FIT-time alignment, overlay editing, and transparent MOV export, in both
  1440 × 900 and 2880 × 1800 RGB PNG formats.
- Added a deterministic manifest-driven generator, shared telemetry backdrop,
  source captures, copy standard, layout rules, privacy review checklist, and
  asset provenance so future locales and releases can reuse one template.
- Replaced the button-like brand capsule with an App Icon and product-name
  lockup, and added explicit wide-gamut-to-sRGB conversion so screenshot UI
  colors remain faithful after compositing.
- Added an antialiased rounded Alpha mask to the marketing App Icon lockup so
  the production icon's opaque black corner pixels reveal the telemetry
  background instead of appearing as a square.
- Replaced the README's older editor captures with a compact App Store gallery:
  one full-width import/alignment hero followed by side-by-side overlay editing
  and transparent export images.
- Verification: deterministic regeneration with
  `python3 scripts/generate-appstore-screenshots.py`; `./scripts/check.sh`;
  `./scripts/publication-audit.sh`; full visual review of all six outputs.

### v0.1.4 Release Preparation

- Advanced release metadata to `0.1.4` with App Store build number `7` for the
  GitHub Release package and the next App Store submission archive.
- Added the required handwritten `v0.1.4` GitHub Release notes for the
  Interval workout overlays, canvas sizing, editing, import, and export
  feedback improvements included in this release.
- Verification: `./scripts/check.sh`; `./scripts/validate-appstore-config.sh`.

### Command-Line Debug File Imports

- Added optional `--fit <path>` and repeatable `--video <path>` launch arguments for
  `swift run RunningOverlay` so local debugging can load prepared activity and
  media files without opening either file picker.
- Routed startup files through the same project import methods as interactive
  imports, with support for absolute, current-directory-relative, and `~`
  paths.
- Verification: `./scripts/test.sh AppLaunchCommandTests` (5 tests passed);
  `./scripts/check-doc-links.rb`. Full `./scripts/check.sh` is currently
  blocked by unrelated in-progress Interval Countdown edits on this branch.

### Export Completion Feedback

- Replaced the toolbar's completed `100%` export display with a clear green
  `Export Complete` confirmation, while retaining item-level export detail in
  the progress popover.

### Interval Text Inspector Grouping

- Moved Work Summary text-role visibility switches into the trailing role-group
  headers and removed their duplicate standalone rows.
- Updated Countdown Text to use the same role-group structure, with short row
  labels and trailing visibility switches; the redundant Countdown-only
  visibility section is removed.

### Interval Countdown Center Value

- Added Center Value metric choices for current-segment time or distance, with
  independent `End to 0` and `0 to End` directions.
- Center values and captions now resolve elapsed/remaining semantics consistently
  in preview, SwiftUI export, legacy frame rendering, and templates.

### Interval Countdown Ring Direction

- Added a Progress Ring `Direction` control for Interval Countdown. Clockwise
  keeps the existing remaining-time treatment (`Full to Empty`), while
  counterclockwise fills elapsed time from `Empty to Full`.
- Preview, SwiftUI export, legacy frame rendering, project/template persistence,
  component guidance, and regression coverage use the same direction setting.

## 2026-07-13

### Separate Project Aspect Ratio And Resolution

- Added six project aspect-ratio choices covering 16:9, 4:3, and 1:1 in
  landscape and portrait workflows, with four matching resolution tiers per
  ratio.
- Project Settings now selects Aspect Ratio before Resolution and preserves the
  current short-edge tier when the ratio changes.
- Export now displays Aspect Ratio read-only and limits Resolution choices to
  the selected project ratio.
- Added backward-compatible settings decoding that infers aspect ratio from
  legacy saved resolutions, plus regression coverage for presets, migration,
  ratio changes, and 1:1 persistence.
- Verification: `./scripts/test.sh ProjectSettingsTests`;
  `./scripts/test.sh ProjectDocumentUndoTests/projectAspectRatioAndResolutionChangesAreUndoable`;
  `./scripts/check.sh`.

### Timeline Marquee Selection And Batch Delete

- Added mouse marquee selection from empty timeline time-area space, including below the last video track, with live selection feedback across timeline layers.
- Added multi-clip project selection and Delete/Forward Delete handling that removes all selected clips in one undoable edit and refreshes linked Media Pool alignment states.
- Added timeline-model and project undo regression coverage for cross-layer batch deletion.
- Verification: `./scripts/test.sh TimelineModelTests`; `./scripts/test.sh ProjectDocumentUndoTests`; `./scripts/check.sh`.

## 2026-07-08

### Interval Countdown Design Spec

- Added the Interval Countdown overlay concept as a compact circular current
  segment countdown for structured workouts.
- Expanded the design guidance with per-role text visibility, typography,
  group-color following, progress-ring glow, shared/global glow, and
  background-dependent shadow rules.
- Added a structured UI spec to guide future implementation and linked it from
  the design index.
- Verification: `./scripts/check-doc-links.rb`.

### Interval Countdown Overlay

- Added `Interval Countdown` as a new interval-series overlay with a circular
  remaining-time progress ring, current lap countdown text, optional helper /
  phase / rep / caption text, and group-color following.
- Added preview, SwiftUI export, legacy frame-renderer, Overlay Pool, Inspector,
  Codable style, and reset/default-style wiring.
- Aligned the Countdown / Progress Ring / Text inspector controls with the
  shared dense inspector row system used by Background, Border, and Effects.
- Documented the module implementation and shared Background / Border / Effects
  behavior.
- Verification: `./scripts/test.sh OverlayRenderModelTests/intervalCountdownLayoutUsesCurrentLapRemainingTimeAndGroupColor`;
  `./scripts/test.sh OverlayRenderModelTests/intervalCountdownCanHideEveryTextRoleExceptCountdown`;
  `./scripts/test.sh OverlayRenderModelTests/overlayFrameRendererWritesIntervalCountdownPNG`;
  `./scripts/test.sh OverlayTemplateTests/intervalCountdownStyleDecodesFromOlderOverlayStyleAndEncodes`;
  `./scripts/check.sh`.

### Interval Work Summary Design And Overlay

- Added the Interval Work Summary design concept as a short-lived post-work-lap
  summary card with one primary metric and up to three secondary metrics.
- Added design documentation, a structured UI spec, and module documentation for
  trigger timing, metric selection, typography, label visibility, and shared
  Background / Border / Effects behavior.
- Added `Interval Work Summary` as a new interval-series overlay that renders
  only inside the configured display window after an active lap ends.
- Added preview, SwiftUI export, legacy frame-renderer, Overlay Pool, Inspector,
  Codable style, and reset/default-style wiring.
- Added left / center / right alignment control for the summary component label
  while keeping it free of decorative trailing rules.
- Grouped Work Summary Text inspector controls by text role and shortened the
  row labels to avoid wrapping in the narrow inspector.
- Verification: `./scripts/test.sh OverlayRenderModelTests/intervalWorkSummaryShowsCompletedActiveLapInsideDisplayWindow`;
  `./scripts/test.sh OverlayRenderModelTests/intervalWorkSummaryHidesOutsideDisplayWindowAndRespectsSlotVisibility`;
  `./scripts/test.sh OverlayRenderModelTests/overlayFrameRendererWritesIntervalWorkSummaryPNG`;
  `./scripts/test.sh OverlayTemplateTests/intervalWorkSummaryStyleDecodesFromOlderOverlayStyleAndEncodes`;
  `./scripts/check.sh`.

## 2026-07-07

### GitHub Release Notes Workflow

- Added handwritten `v0.1.3` GitHub Release notes matching the established
  release-page style.
- Updated the release workflow to require `docs/releases/${tag}.md` before a
  tag can publish or update a GitHub Release, including validation that the
  note names the uploaded zip and checksum assets.
- Documented the release-note requirement in the release guide.
- Verification: `./scripts/check-doc-links.rb`; `./scripts/check.sh`.

### v0.1.3 Release Preparation

- Advanced release metadata to `0.1.3` with App Store build number `6` for the
  GitHub Release package and next App Store submission build.
- Release includes export orientation-preserving resolution presets, Media Pool
  match/status workflow improvements, Templates footer alignment, and compact
  timeline startup sizing from the current release branch.
- Verification: `./scripts/check.sh`; `./scripts/validate-appstore-config.sh`.

### Compact Timeline Startup Height

- Lowered the timeline split's startup height so the Media Pool, Preview, and
  Inspector panels start with more vertical space, while keeping the timeline
  manually resizable.
- Verification: `./scripts/check-doc-links.rb`; `./scripts/check.sh`.

### Templates Footer Height

- Matched the Templates Pool sticky footer height to the Preview bottom
  playback bar while keeping the import and save buttons at their existing
  32 px control height.
- Verification: `./scripts/check-doc-links.rb`; `./scripts/check.sh`.

### Media Pool Status After Clip Delete

- Deleting a timeline clip now refreshes the linked Media Pool item status so
  timestamp-matchable media returns from green to yellow and manually placed
  media returns to the muted manual-placement state.
- Verification: `./scripts/test.sh ProjectDocumentUndoTests/deletingTimestampMatchedClipReturnsMediaStatusToReadyToMatch`;
  `./scripts/test.sh ProjectDocumentUndoTests/deletingManuallyPlacedClipReturnsMediaStatusToNeedsManualPlacement`.

### Media Pool Status Dot Help

- Media Pool alignment status dots now expose clearer hover help text and a
  larger hover target so yellow, green, and muted status dots explain whether
  media is ready to match FIT timestamps, already on the timeline, or needs
  manual placement.
- Verification: `./scripts/test.sh ProjectDocumentUndoTests/mediaAlignmentStatusHelpTextExplainsStatusDots`.

### Media Pool Match to Existing Layer

- Added a `Match to Layer` submenu to Media Pool context menus so selected
  media can be timestamp-matched directly onto an existing timeline layer.
- Verification: `./scripts/test.sh ProjectDocumentUndoTests/matchingMediaToExistingLayerUsesSelectedLayerAndIsUndoable`.

### Export Resolution Picker

- Export dialog resolution is now editable from three same-orientation presets,
  so landscape projects can choose only landscape export sizes and portrait
  projects can choose only portrait export sizes.
- Verification: `./scripts/test.sh ProjectSettingsTests`.

## 2026-07-06

### HIG UI Validation Workflow

- Added a design workflow for running Codex HIG skill-based UI validation
  against the macOS app, including skill routing, surface inventory, static and
  runtime passes, accessibility checks, finding format, and validation commands.
- Linked the workflow from the design documentation index.
- Verification: `./scripts/check-doc-links.rb`.

### Custom Numeric Overlay

- Added a Custom Numeric overlay tile that uses the existing Numeric Overlay
  Inspector and shared preview/export text render path.
- Custom Numeric stores a selectable FIT-derived value field, format category,
  decimal precision, and free-form unit suffix with Codable defaults for older
  projects/templates.
- Imported FIT record messages now retain generic scalar numeric fields so
  Custom Numeric can expose fallback fields such as `record.field_87` and raw
  developer field values.
- The Custom Numeric value menu now filters fields already covered by fixed
  Numeric Overlays, while retaining latitude/longitude and non-duplicative
  generic record fields.
- Added formatter, undo/Codable, and render-model regression coverage for
  custom fields such as heart rate, distance, elapsed time, timestamps, GPS
  coordinates, and generic record fields.
- Verification: `./scripts/test.sh OverlayValueFormatterTests`;
  `./scripts/test.sh FitFileParserTests`;
  `./scripts/test.sh ProjectDocumentUndoTests`;
  `./scripts/test.sh OverlayRenderModelTests`;
  `./scripts/test.sh SFSymbolCatalogTests`.

### Lap Pace Minute-Boundary Formatting Fix

- Fixed pace formatting so numeric overlays and Interval HUD Bar lap-pace
  metrics use one rounded total-second value for both minutes and seconds.
  Values between `3'59"` and `4'00"` now render as `4'00"` instead of `3'00"`.
- Added regression coverage for the Numeric Overlay formatter and Interval HUD
  Bar lap-pace metric path.
- Verification: `./scripts/test.sh OverlayValueFormatterTests/lapPaceRoundsAcrossMinuteBoundary`;
  `./scripts/test.sh OverlayRenderModelTests/intervalHUDBarLapPaceRoundsAcrossMinuteBoundary`.

## 2026-07-05

### v0.1.2 Release Preparation

- Advanced release metadata to `0.1.2` with App Store build number `5` for the
  GitHub Release package and next App Store submission build.
- Release includes FIT interval lap classification fixes, local-development
  Keychain prompt avoidance, FIT replacement auto-match, and Interval HUD/Timeline
  4K export scaling parity from PR #13.
- Verification: `./scripts/check.sh`; `./scripts/validate-appstore-config.sh`.

## 2026-07-04

### Interval HUD Background 4K Export Parity

- Moved Interval HUD background padding, background radius, border width,
  divider thickness, bottom-bar spacing, bottom-bar corner radius, and
  bottom-bar border width into the shared render layout with canvas scaling.
- Updated SwiftUI preview/export and the legacy frame renderer to consume those
  shared dimensions so 4K exports keep HUD rounded corners and borders
  proportional to preview.
- Verification: `./scripts/test.sh OverlayRenderModelTests/intervalHUDBarBackgroundGeometryScalesWithCanvasSize`.

### Interval Timeline 4K Export Parity

- Moved Interval Timeline marker width, marker font size, marker label bounds,
  segment corner radius, current-segment border width, and outer border width
  into the shared render layout with canvas scaling.
- Updated SwiftUI preview/export and the legacy frame renderer to consume those
  shared dimensions so 4K exports keep `NOW`, marker arrows, and white borders
  proportional to preview.
- Verification: `./scripts/test.sh OverlayRenderModelTests/intervalTimelineMarkerAndBorderScaleWithCanvasSize`.

### Interval HUD Marker Export Parity

- Shared Interval HUD zone-marker sizing between preview and the legacy frame
  renderer so exported marker arrows and value labels no longer shrink relative
  to the preview.
- The legacy renderer now draws the current zone-marker value label with the
  same padded dark background used by the SwiftUI preview.
- Verification: `./scripts/test.sh OverlayRenderModelTests/intervalHUDBarZoneMarkerMetricsMatchPreviewAndExportScale`.

### Replace FIT Auto-Matches Existing Videos

- Replacing a FIT file now recalculates existing media items' timestamp match
  status against the new activity and automatically places timestamp-matched
  videos on their camera-group timeline tracks when they do not overlap existing
  clips.
- Added a regression test for importing videos against the wrong FIT, then
  replacing the FIT and getting timestamp-aligned clips without a separate
  manual auto-match action.
- Verification: `./scripts/test.sh ProjectDocumentUndoTests/replacingFitAutoMatchesExistingTimestampedVideos`.

### Local Development Keychain Prompt Avoidance

- Added a default credential-store factory that keeps bundled app builds on the
  macOS Keychain but uses an in-memory OpenWeather credential store when the app
  is launched without a bundle identifier, such as `swift run RunningOverlay`.
- Added `RUNNING_OVERLAY_DISABLE_KEYCHAIN=1` as an explicit development escape
  hatch for local runs that should never touch Keychain.
- Documented the local-development behavior in `docs/development/persistence.md`.
- Verification: `./scripts/test.sh ProjectSettingsTests`.

### Interval HUD FIT Lap Classification Fix

- Updated FIT lap import to ignore zero-duration, zero-distance lap messages so
  Interval HUD Bar and Interval Timeline do not render phantom `0:00` cooldown
  segments.
- Refined interval workout analysis so mixed-distance work after a repeated
  interval set can inherit active semantics when its pace matches the detected
  work laps, with the following recovery lap kept as rest when a cooldown lap
  remains.
- Refined repeat detection to try alternate distance and duration candidates
  when the largest repeated-distance group does not produce separated work/rest
  intervals, covering short reps such as 200 m x 20 where recovery distances can
  be close to work distances.
- Added a guard against classifying ordinary paused auto-lap runs as structured
  intervals when duration-based candidates are just repeated long-distance laps
  with pause-stretched kilometers.
- Added synthetic parser and workout-structure regression tests. Verified with
  an optional local FIT sample without committing private activity data.
- Verification: `./scripts/test.sh FitFileParserTests`;
  `./scripts/test.sh WorkoutStructureAnalyzerTests`;
  `RUNNING_OVERLAY_INTERVAL_FIT_SAMPLE=/path/to/local.fit ./scripts/test.sh WorkoutStructureAnalyzerTests/parsesProvidedSlowIntervalFitSampleWhenAvailable`;
  `RUNNING_OVERLAY_NORMAL_FIT_SAMPLE=/path/to/local.fit ./scripts/test.sh WorkoutStructureAnalyzerTests/parsesProvidedNormalPausedFitSampleWhenAvailable`.

## 2026-07-02

### Mac App Store Badge URL Fix

- Replaced the broken README Mac App Store badge asset URL with the Apple
  Marketing Tools badge API URL so GitHub renders the badge instead of a
  missing-image placeholder.
- Verification: fetched the badge API URL and confirmed it returns
  `image/svg+xml`; `./scripts/check-doc-links.rb`.

### Mac App Store Launch README Update

- Added the official Mac App Store badge and public App Store link for Running
  Overlay Studio to the top of the README.
- Updated the release availability note to identify the Mac App Store as the
  signed distribution channel while keeping source and GitHub release package
  guidance.
- Verification: documentation-only change; no build or test run.

## 2026-07-01

### COROS Lap Calories Import

- Updated FIT parsing so record-level calories remain authoritative, while files
  that omit record calories can estimate cumulative calories from lap
  `total_calories`. This covers COROS exports that provide session and lap
  calorie totals but no direct per-record calorie field.
- Added a synthetic in-memory parser regression test for lap-calorie-only FIT
  data without committing private activity files.
- Verification: `./scripts/test.sh FitFileParserTests`;
  optional local COROS sample via `RUNNING_OVERLAY_FIT_SAMPLE`;
  `./scripts/check.sh`.
